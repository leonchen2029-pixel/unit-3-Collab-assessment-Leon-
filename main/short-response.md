1. **If you are collaborating with a classmate what is the first thing that each of the team members needs to do? Why is it important to do this step?**

The team members need to create new branches because if they were coding on the same branch they would interfere with each other.

2. **Why do developers use branches?**

Developers use branches because they have to avoid interfering with the other developers. If they didn't branch, they would use the same branch and one dev's work will interfere with another dev's work.

3. What is git? What is github? How does git and github work together?

Git is a distributed version control it is used to save code and for many devs to create different code. Github is a cloud based service that allows devs to manage and control changes to their work. It is used to store code, control code and make collabtation easier. Git and Github work together by making collabration easier. Git helps developers create multiple branches of code while github helps developers to store code and to control the code.

4. 